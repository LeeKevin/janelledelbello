{
  "name": "janelledelbello",
  "version": "1.0.0",
  "description": "",
  "devDependencies": {
    "babel-preset-es2015": "^6.18.0",
    "font-awesome": "^4.7.0",
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^3.1.1",
    "gulp-inject": "^4.1.0",
    "gulp-sass": "^3.0.0",
    "gulp-sourcemaps": "^1.9.1",
    "rimraf": "^2.5.4"
  },
  "scripts": {
    "build": "npm run clean && gulp build",
    "watch": "npm run clean && gulp watch",
    "clean": "rimraf ./dist"
  },
  "dependencies": {
    "normalize-scss": "^6.0.0"
  }
}
